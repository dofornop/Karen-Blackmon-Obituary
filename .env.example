# Password for the hidden /admin page used to delete guestbook messages.
ADMIN_PASSWORD=change-me

# Set automatically by Vercel when you add a Postgres (Neon) storage
# integration to the project. Not needed for local development — the app
# falls back to a local SQLite file at .local-data/guestbook.db.
# DATABASE_URL=
